{
  "collectionName": "components_blocks_editor_picks",
  "info": {
    "displayName": "Editor Pick",
    "icon": "bookmark",
    "description": "Curated bundle of hand-picked articles (featured series, themed selections)."
  },
  "options": {},
  "attributes": {
    "headline_ar": {
      "type": "string"
    },
    "headline_en": {
      "type": "string"
    },
    "subhead": {
      "type": "text"
    },
    "mode": {
      "type": "enumeration",
      "enum": ["hand-picked", "flag-driven"],
      "default": "hand-picked"
    },
    "articles": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::article.article"
    },
    "limit": {
      "type": "integer",
      "min": 1,
      "max": 12,
      "default": 4
    },
    "layout": {
      "type": "enumeration",
      "enum": ["two-up", "three-up", "magazine"],
      "default": "three-up"
    }
  }
}
