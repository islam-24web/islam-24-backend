# Override Railpack/Nixpacks to use npm install instead of npm ci
# لأن npm ci يحتاج lockfile مثالي - npm install يحل التبعيات بمرونة

[phases.install]
cmds = ["npm install --legacy-peer-deps --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
